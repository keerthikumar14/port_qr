<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>Confromation page</title>
</head>
<body>

    <div class="container" style="margin-top: 30px;">
        
    <% if(id!='undefined'){ %>
        <% if(id=='Not provided'){ %>
        <p class="h1" style="color: red;"><%= id %></p>
        <% } %>
        <% if(id!='Not provided'){ %>
            <p class="h1" style="color: green;"><%= id %></p>
            <% } %>
        <% } %>
    <% if(id=='undefined'){ %>
            
            <% } %>
    <p></p>
    <p></p>
    <p></p>

    <% if(n!='undefined'){ %>
    <p class="h5">Name : <%= n %></p>
    <% } %>
    
    <% if(n=='undefined'){ %>
        <p class="h2">No record</p>
        <% } %>


            <% if (rg) { %>
      
                <form action="/change_state" method="post">
                    <input type="text" name="reg_no" value="<%= rg %>" readonly>
                    <input type="submit" class="btn btn-outline-success" value="Provide">
                </form>
                <% } %>
        
            
        
    
</div>
  <p></p>
  <p></p>
    <div class="container">
        <a href="/" style="text-decoration: none;" class="btn btn-warning">Back to Scanner</a>
</div>
</body>
</html>